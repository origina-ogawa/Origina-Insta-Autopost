# 進捗

作業を始める前に必ずこのファイルを読むこと。作業の節目(タスク完了、方針決定、コミット・PR作成など)で必ず更新すること。
自動圧縮でスレッドの記憶が失われても、ここを読めば状況を復元できるようにする。

最終更新: 2026-08-14(3Dオフィス表示層(viewer/)のデスクグリッド化・社長席新設・個人名付与・吹き出し再改修を完了し、mainへローカルマージ済み)

## 今の状況

- **PR #1〜#8はすべて社長がマージ済みで、`main` に反映済み。** 以前この進捗.mdには「PR #4〜#7はレビュー待ち」と書かれていたが、GitHub上の実際の状態(`gh pr list --state all`)で確認したところ全件マージ済みだったため、記載を修正した(いつマージされたかまでは`gh`のログから遡れるが、この修正時点で判断の詳細までは追えていない)
- マージ済みブランチ(feat/office-viewer-tatami, feat/ai-company, feat/agent-event-logging, feat/sources-work-scaffold, feat/producer-render-schema-bridge, docs/update-progress-2026-08-13, claude/ecstatic-dirac-24eb2a)はリモート・ローカルとも削除済み。使われていなかった古いワークツリー(ecstatic-dirac-24eb2a, gifted-moser-7e7b02)も削除済み

## 済み: 3Dオフィス表示層(viewer/)のデスクグリッド化・社長席新設・個人名付与・吹き出し再改修(2026-08-14)

前回のUI改修(直下の節参照)の後、実際に画面を見た社長からの追加フィードバック3点を
brainstorming→writing-plans→subagent-driven-developmentの流れで実装。設計書・計画書は
`docs/superpowers/specs/2026-08-14-office-viewer-grid-and-names.md` /
`docs/superpowers/plans/2026-08-14-office-viewer-grid-and-names.md`参照。

- **吹き出しの再改修**: 前回直したはずの吹き出しの幅がまだ文字より狭かった原因は、
  全角/半角を区別せず一律の幅で計算していたこと(日本語主体のメッセージは常に狭く
  見積もられていた)。全角=1.0・半角=0.55の重み付けに修正。あわせて吹き出しの形状も
  角ばった長方形から、角丸+下向きのしっぽ付きの「吹き出しらしい」形状(THREE.Shape
  で構築)に変更
- **デスクレイアウトを横3×縦2グリッドに変更+社長席を新設**: 半円配置だと右側の
  サイドバー(アクティビティログ)に机が隠れる問題があったため、AI社員6人(PM含む)を
  グリッドに再配置。PMの旧位置(奥中央の特等席)には、ユーザー(社長)を表す飾りの机
  「社長席」を新設(イベントログとは非連動、アバター無し)。カメラも左右シフトして
  サイドバーとの重なりを回避
- **AI社員6人に個人名を付与**: 職業から連想したひらがな名(PM=つかさ、リサーチ=あかり、
  ディレクター=ゆい、制作=たくみ、目利き=さえ、配信=つばさ)を割り当て、名札を
  「名前(大)+役職(小)」の2行表示に変更
- **実装方式**: 前回と同じくsubagent-driven-development(隔離worktree+タスクごとの
  実装→レビューのラウンドトリップ)。今回は計画段階で数値(吹き出しサイズの期待値、
  WALK_DIRの座標変換結果)を実際にNodeで実行して事前検証したため、前回のような
  計画書側の手計算ミスは発生しなかった
- **最終の全体レビュー(opus)で見つかった3件のImportant不具合**(実装者のミスではなく
  計画書側の見落とし。修正済み・再レビュー済み):
  1. 吹き出しの`ShapeGeometry`がアンマウント時に破棄されず、常時稼働するビューアで
     メモリリークになっていた(角丸形状化に伴う新規混入。`geometry.dispose()`を
     `useEffect`のクリーンアップで呼ぶよう修正)
  2. 社長席がディレクター席の真後ろに重なり、ディレクターが喋るたびに名札が隠れて
     いた(カメラの向きの都合。社長席のx座標を0→1.8に調整し、ディレクター列と
     producer列の隙間に収まるようにした)
  3. `viewer/README.md`が旧仕様(半円配置・4パネル構成)のままだったため更新
  - あわせて発見された「グリッド化に伴い、中央列(director/publisher)の出社/退社
    アニメーションが右列(producer/pm)の机を一直線に貫通してしまう」問題は、
    専用の設計サイクルが必要な変更のためコード修正はせず、README「今できていない
    こと」に既知の制限として記録する判断とした
  - 名札コンポーネントの重複(NameSign/PresidentNameSign)を統合、吹き出しの長文時に
    アバターの頭を覆ってしまう問題を修正(高さが伸びた分は上方向に広がるよう変更)、
    テストカバレッジ追加(中央列アクターのWALK_DIR)などの細かい改善も同時に実施
- mainへローカルマージ済み(fast-forward)。マージ後に`npm test`(11/11)・`npm run build`
  とも確認済み。worktreeは削除済み。origin/mainへのpushは未実施(社長の確認待ち)

## 済み: 3Dオフィス表示層(viewer/)のUI改修(2026-08-14)

社長からの要望8点を整理し(4点はviewer/のUI改修として今回実施、残り4点はスコープ外・別途検討)、
brainstorming→writing-plans→subagent-driven-developmentの流れで実装。設計書・計画書は
`docs/superpowers/specs/2026-08-14-office-viewer-ui-update.md` / `docs/superpowers/plans/2026-08-14-office-viewer-ui-update.md`参照。

- **実施した4点**:
  1. アクティビティログの文字切れ修正(折り返し表示に変更)
  2. 社員の出社/退社タイミング見直し(イベント発生時に机の左右から歩いて出社、`done`で歩いて退社し机だけになる)
  3. 凡例(レジェンド)パネルの削除
  4. 吹き出しのはみ出し修正(メッセージ長に応じてサイズを動的計算)+ 役職ごとの「面白いセリフ」をランダムに喋る演出を追加(実イベントメッセージが常に優先)
- **スコープ外とした4点**: producer(制作)の表示ラベル変更は不要と確認(既に表示されていた)。Claude Codeの状態表示・指示入力欄の2点は、現状のviewerが`logs/events.jsonl`を読むだけの一方向の仕組みであることから、実現方式の検討が必要な別プロジェクトとして今回のスコープ外にした(要望7・8、未着手)
- **実装方式**: `.claude/worktrees/`配下に隔離worktreeを作成し、6タスクに分解した実装計画をsubagent-driven-developmentで実行(タスクごとにimplementer→reviewerのラウンドトリップ)。純粋ロジック部分(吹き出しサイズ計算・出社退社の方向ベクトル)はNode標準テストランナー(`node --test`、新規パッケージ追加なし)でTDD検証、3D描画・アニメーション部分は`npm run build`の型チェック+`npm run dev`での目視確認
- **見つかった不具合と対応**:
  - Task 2で吹き出しサイズ計算(`computeBubbleSize`)の行数算出に丸め誤差のCritical不具合(実装者が`round()`を使い一部メッセージ長で行数を過小評価)。計画書自体の検証用期待値の手計算ミスが原因と判明し、`floor`/`ceil`方式に修正して解消
  - 最終の全体レビュー(opus)で2件のImportant不具合を発見。いずれも実装者のミスではなく計画書の設計不備:
    1. 退社アニメーションの歩行距離が固定値5だったため、6人中4人(pm/producer/director/inspector)が壁(x=±9)を越えられず部屋の真ん中で消えていた → 全員が同じワールドX(±11)に到達するよう机の位置に応じた距離に変更
    2. 出社/退社アニメーションの「初回一括反映かどうか」の判定がアバターごとの`initializedRef`だったため、ログに履歴が無い社員(現状`publisher`が該当)は初回のライブイベントでも出社アニメーションがスキップされていた → `OfficeState`に`batchCount`を追加し、ページ読み込み時の最初のイベントバッチかどうかで判定する方式に変更
  - `viewer/README.md`が旧仕様(4パネル構成・opacityフェード)のままだったため更新。他、死んだコード(doneバッジ等)の整理も実施
- 実装中、`pkill -f vite`で他のセッションのdevサーバーを誤って停止させてしまった可能性がある(該当タスクの実装者が自己申告)。今回の被害は無かった模様だが、以後の手動確認では特定PIDのみを停止するよう申し送り済み
- mainへローカルマージ済み(マージコミット`9d326fc`)。マージ後に`npm test`(9/9)・`npm run build`とも確認済み。worktreeは削除済み

## 済み: AI社員パイプライン(第1〜4幕)の初回エンドツーエンド実行(2026-08-14)

3Dオフィス表示層で実際の動作を見ながら、pm→researcher→director→producer→inspectorを初めて通しで実行(テーマ: 「直近のAIエージェント関連アップデート4件」、`sources/2026-08-14-ai-agent-platform-updates.md`)。

- **確認できたこと**: 各エージェントが自分の役割定義(.claude/agents/*.md)通りに`scripts/emit-event.mjs`を実際に呼び、3Dビューにも反映された(旧・残タスク「エージェントが実際にemit-event.mjsを呼ぶか」を確認できた)。researcherは鮮度基準を満たせないテーマ(金曜『SNS集客術』カテゴリ)で捏造せず`blocked`し、PM判断で木曜『AIエージェント最前線』カテゴリに差し替えて再実行し成功。directorは社長フィードバック(経営者目線への書き直し)を反映して構成案を修正。inspectorは1回目、文字数などの表面OKに満足せず画像を実際に見て不合格判定を出した
- **見つけて修正した2件のレンダリングバグ**(このパイプライン特有ではなく、キャラクター機能(PR #8)を使う投稿全般に影響する既存バグだった):
  1. `src/lib/components.js`: キャラクター表示時、`.para`の余白巻き戻し(`margin-right: -200px`)が`lead`ブロック用に設計されたものだったが、CSSクラスが同じため`paragraph`ブロック(AI社員パイプラインが使う汎用ブロック)にも適用され、4行に近い本文がキャラクター画像と重なって見切れていた。`lead`ブロック限定に修正
  2. `scripts/slides-to-post.mjs`: 表紙見出しの機械的な改行が8文字区切りだったが、96pxフォント×キャラクター表示時640px幅制約では8文字が収まらずブラウザ側でさらに折返され、マーカー(黄色ハイライト)がずれていた。6文字区切りに変更(該当テストも更新)
  3. (バグではなく表記の食い違い)`rubric/carousel.md`の5-1と`templates/carousel/tokens.json`のcanvasが「1080×1350(4:5)」だったが、実際の`src/render.js`は`1080×1080`固定(CLAUDE.mdの正式仕様と一致)。ルーブリック・tokens.json側を1080×1080に修正。**tokens.jsonの`canvas`/`safeArea`フィールドはコードから一切参照されておらず、実質ドキュメントのみの値**という点も判明(font/limitsは実際に上限として使われている)
  - 修正後、producerが本文を再調整(2〜6枚目を44〜49字に短縮)し再検収 → 合格(第2回)。`npm test`は28件全て成功
- **投稿承認フローの変更(2026-08-14決定)**: 従来は「publisherがPRを作成→社長がマージ」だったが、「第2幕の構成承認」と「第4幕inspectorの検収」の2段階チェックで十分と社長が判断し、**PRを作らずmainへ直接push**する運用に変更した。AGENTS.md 6条・CLAUDE.scaffold.md・`.claude/agents/publisher.md`を改訂済み。今後Issue自動起動などで無人実行される場合もこの方式が適用される(=投稿直前の人間チェックは無くなる。第2幕の構成承認だけが人間の関与ポイントになる点に注意)
- **第5幕まで完走し、実際にInstagramへ投稿完了**: `posts/2026-08-14-ai-agent-platform-updates/`をmainへpush → `publish.yml`が自動発火 → 投稿成功(`media_id: 18110940545061235`)。`published.json`もActions側で自動コミットされ、ローカルにpull済み
- コミットは3つに分割: `2ff9f55`(レンダリングバグ修正)→`2091961`(承認フロー変更)→`de55462`(投稿コンテンツ、push後Actionsが`c014710`でpublished.json追加)

## 済み: 3Dオフィス表示層(viewer/)

PR #3として作成し、社長がマージ済み(`main`に反映済み)。技術構成・イベント演出・既知の制限は
下の「完了したこと(3Dオフィス表示層)」および `viewer/README.md` を参照。

## 済み: 社長外出中に作成された4件のPR(PR #4〜#7)

いずれも `main` から新しくブランチを切って作成され、その後マージ済み。

### PR #4: [director/inspector/publisherにイベント記録を追加](https://github.com/origina-ogawa/Origina-Insta-Autopost/pull/4)
- `.claude/agents/pm.md`/`producer.md`/`researcher.md` には既にあった「## 記録」セクション(節目で `scripts/emit-event.mjs` を呼ぶ指示)を、`director.md`/`inspector.md`/`publisher.md` にも追加
- director: 構成案output時とwaiting突入時にblocked / inspector: 不合格時reject・合格時done / publisher: PR作成時output・停止時done
- 確認ポイント: イベント種別とタイミングの割り当てが `logs/SCHEMA.md` の意図と合っているか(`handoff`は使わず`output`/`blocked`/`done`のみにした理由をPR本文に記載)

### PR #5: [sources/・work/の雛形とブランチ運用ドキュメントを追加](https://github.com/origina-ogawa/Origina-Insta-Autopost/pull/5)
- 設計書 `docs/superpowers/specs/2026-08-12-act5-publish-trigger-design.md` の未解決事項「第1・2幕のディレクトリ運用整備」への対応。**雛形とドキュメント追加のみに留め、大きな構造変更はしていない**
- `sources/README.md`(命名規則)、`work/.gitkeep`(+`.gitignore`修正)、`docs/superpowers/specs/2026-08-13-sources-work-scaffold.md`(ブランチ運用の整理と未解決事項)
- 併せて「幕1〜4のIssue自動起動化は今は見送る」決定メモ(`docs/superpowers/specs/2026-08-13-issue-auto-trigger-decision.md`、実装なし)も同じPRに含めた
- **未解決のまま残っている論点**: `post/YYYY-MM-DD-<slug>` ブランチをどの幕・どのタイミングで切るか(researcher開始時 or director承認後、の2案。PR本文・設計書に詳細)。マージはされたが、この設計自体は決め切れていない(下記「残タスク」参照)

### PR #6: [producerのslides.jsonとrender.jsのpost.jsonをつなぐ変換スクリプトを追加](https://github.com/origina-ogawa/Origina-Insta-Autopost/pull/6)
- `producer.md` の `slides.json`(heading/body形式)と、本番の `render.js` が読む `post.json`(cover/body/summary+blocks形式)の食い違いを解消するアダプタ `scripts/slides-to-post.mjs` を新設。**`render.js`自体は無変更**(追加のみ)
- 検討した3案(アダプタ追加/render.js側で分岐/producer.mdの出力仕様自体を作り直す)とその判断理由をPR本文に記載。今回はアダプタ追加を採用し、他の2案は将来の選択肢として書き残した
- 変換は「1枚目→cover、最後→summary、間→body」で各1ブロックの単純な構成にする割り切り。checklist/compareのような装飾は使えず、見た目は`generate.js`(Gemini)経由よりシンプルになる
- ローカルで `npm run generate -- --mock` → `npm run render`(既存経路が無事動くこと)、および サンプルslides.json → `node scripts/slides-to-post.mjs` → `npm run render`(変換後も崩れず5枚生成)を確認済み。`npm test` 25件全て成功
- **未解決のまま残っている論点**: この「シンプルな見た目」の割り切りで進めてよいか。将来もっとリッチな構成にしたい場合は上記3案のうち他の2つを検討することになる(下記「残タスク」参照)

### PR #7: 進捗.md更新(社長外出中の状況整理)
- 上記PR #4・#5・#6を作成した経緯と確認ポイントを`進捗.md`に記録したもの。中身は本ファイルの当時の版に相当

## 済み: カルーセルデザイン改修+解説キャラクター機能(PR #8)

PR #8として作成し、社長がマージ済み(`main`に反映済み)。設計書: `docs/superpowers/specs/2026-08-13-character-slide-design.md`、実装計画: `docs/superpowers/plans/2026-08-13-character-slide.md`。詳細は下の「完了したこと(カルーセルのデザイン改修+解説キャラクター機能)」を参照。

- **記録**: `assets/mirai01.png`/`assets/mirai-ad.png`の人物は、社長ご自身がAIで生成した架空の人物とのご説明を前提に実装(実在人物の肖像は使用していない)
- サブエージェントによるタスクごとのレビュー+最終ブランチレビュー(opus)済み。Important指摘3件を修正済み(表紙タイトル幅のスコープ漏れ、白背景の重なり、ポーズ画像のアスペクト比不統一)
- **マージ後の追加修正(社長フィードバック、2026-08-14に4ラウンド実施)**:
  1. 表紙は人物上部の空白が目立っていたのでタイトル幅を520→640pxに広げ、縦位置を中央寄せ→上詰めに変更。本文スライドはキャラクター表示時に文字が小さくなりすぎていたので6枚目(まとめ)相当のサイズまで拡大し、パネルの右側余白を320→280pxに縮小。キャラクター画像は表紙・本文とも一回り拡大。**白背景を実際に透過(アルファチャンネル)化**する`scripts/remove-character-background.mjs`を新規作成して`assets/character/pose-*.png`に適用し、応急処置だった`mix-blend-mode: multiply`は削除。背景除去には`sharp`をdevDependencyとして追加(理由: PNGの生ピクセルを閾値処理してアルファ値を書き換える必要があり、手書きコードは非現実的。実行は手動一回限りのスクリプトで自動投稿フローでは使わない)
  2. `pose-05.png`は他5枚と解像度が違い、髪の生え際から上が写っていない欠陥画像だったため頭が切れて見えていた。社長が差し替え画像を用意し、同スクリプトで透過処理して解決。チェックリスト型ブロックの不自然な余白を解消(帯と項目群をまとめて中央寄せ)、帯文字も拡大。表紙のキャラクター位置を右へ移動
  3. leadブロックの本文はキャラクターの頭より上に出るため重ならないと判断し、パネルの右余白を一部取り戻して1行に収まりやすくした
  4. 7枚目(ブランド固定スライド)に解説キャラクターを追加。`assets/mirai-ad.png`はスタジオの複雑な背景で閾値処理による透過ができなかったため、社長がPhotoshopで切り抜いた`assets/character/mirai-ad-touka.png`(アルファチャンネル付き)をそのまま使用(依存パッケージの追加は無し)。タグライン・ロゴを縮小しキャラクターを右下に配置。その後さらに右へ移動しテキストとの間隔を拡大

## 残タスク

優先度の高い順。

1. ~~【現在ここ・要判断】`posts/2026-08-14-ai-agent-platform-updates/` が検収合格済み。第5幕(publisher)でPRを作るか~~ → **2026-08-14完了。** PR方式ではなくmain直接push方式に変更した上でpush → `publish.yml`実行 → Instagram投稿成功(`media_id: 18110940545061235`)
2. ~~【要事前確認】`publish.yml` の動作確認・実際の投稿を伴うテスト~~ → **2026-08-14完了。** pushトリガー経路で実投稿を確認済み
3. **(PR #5参照)未解決: `post/YYYY-MM-DD-<slug>` ブランチをどの幕・どのタイミングで切るか** → 2026-08-14の運用変更でブランチを切らずmain直接作業になったため、**この論点自体が解消(そもそもブランチを切らない)**
4. **(PR #6参照)スキーマ橋渡しの「シンプルな見た目」の割り切りで進めてよいか** → 2026-08-14の実行で、キャラクター機能との組み合わせで実際にバグが出ることが判明し修正済み(上記「AI社員パイプラインの初回エンドツーエンド実行」参照)。単純な構成で運用する分には動作確認できたので、割り切り自体は当面維持でよさそう
5. ~~未着手: エージェントが実際に `scripts/emit-event.mjs` を呼ぶようにする~~ → **2026-08-14に確認済み。** researcher/director/producer/inspector/publisherとも自分の役割定義通りにイベントを記録し、3Dビューにも反映された
6. **(任意・低優先度)viewer/の細かい改善候補**
   - `handoff` イベントで隣の机へ何かが飛ぶような専用演出(現状は吹き出しのみ)
   - ページ読み込み時の全イベント一括再生による中間状態の欠落(known limitation、README参照)
7. **(任意・低優先度)最終レビューで見つかった軽微な指摘の後始末**
   - `scripts/detect-pending.mjs` の `::warning::` メッセージが `workflow_dispatch` 実行時にも「pushイベントで」と表示される(誤解を招く文言だが機能に影響なし)
   - `DIR_PATTERN` 正規表現が `scripts/detect-pending.mjs` と `scripts/publish-instagram.mjs` の2箇所に重複している(意図的に許容、共有モジュール化はしていない)
8. **未着手: 幕1〜4のIssue自動起動化の検討**
   - PR #5の決定メモにより当面は見送り。実績確認後に再検討する。ただし2026-08-14の運用変更(PR不要・直接push)により、**自動起動を有効にした場合は人間の関与ポイントが「第2幕の構成承認」のみになる**点は再検討時に要考慮
9. **(新規・任意)`sources/README.md`・`docs/superpowers/specs/2026-08-13-sources-work-scaffold.md`など、PR前提で書かれたドキュメントの記述更新**
   - 2026-08-14の運用変更(PR不要)に伴い、PR運用を前提にした古い記述が残っている可能性がある。次回それらを触る際に確認する

## 完了したこと(カルーセルのデザイン改修+解説キャラクター機能)

- `src/lib/components.js`: 本文スライドを2カラム詰め込み→1カラム1要素構成に変更、文字数削減に合わせてフォント拡大
- `src/lib/characterPoses.js`: スライドへのポーズ画像割り当てロジック(summaryスライド除外・循環割当、単体テスト付き)
- `scripts/generate-character-poses.mjs`: 参考画像からポーズ違い画像を生成する手動スクリプト(自動投稿フローには非組込)
- `assets/character/pose-01〜06.png`: 事前生成したポーズ画像(6種、コミット済み)。うち`pose-05.png`は社長差し替え版に更新済み
- `assets/character/mirai-ad-touka.png`: ブランド固定スライド用に社長がPhotoshopで切り抜いた透過済み画像
- `scripts/remove-character-background.mjs`: pose画像の白背景を実際に透過(アルファチャンネル化)する手動スクリプト。ファイル名を指定して1枚だけ処理することも可能
- `src/render.js`: `character`設定を読み込み、スライドごとにポーズ画像をdata URIで埋め込み。ファイル欠落時はエラーで停止。ブランド固定スライド用のキャラクター画像読み込みにも対応
- `config/brand.own.json`: `character`設定(`enabled`/`dir`/`poses`)と`brandSlide.characterImage`を追加
- 最終ブランチレビュー(opus)で見つかったImportant 3件を修正: 表紙タイトルの幅制限がキャラクター無効ブランドにも適用されていた不具合、キャラクター画像の白背景がパネル上に矩形として見えていた問題、ポーズ画像1枚だけアスペクト比が違いサイズが不揃いだった問題
- マージ後、社長フィードバックにより4ラウンドのレイアウト微調整を実施(詳細は上の「済み: カルーセルデザイン改修+解説キャラクター機能」参照)

## 完了したこと(社長外出中の追加実装)

- `.claude/agents/director.md` / `inspector.md` / `publisher.md` に「## 記録」セクション(`scripts/emit-event.mjs` 呼び出し)を追加(PR #4)
- `sources/README.md`(一次ソースの命名規則)、`work/.gitkeep`+`.gitignore`修正、ブランチ運用の整理ドキュメント`docs/superpowers/specs/2026-08-13-sources-work-scaffold.md`(PR #5)
- 幕1〜4のIssue自動起動化を今は見送る決定メモ `docs/superpowers/specs/2026-08-13-issue-auto-trigger-decision.md`(実装なし、PR #5に同梱)
- `scripts/slides-to-post.mjs`: `producer.md`の`slides.json`を`render.js`が読める`post.json`へ変換するアダプタ。`render.js`本体は無変更(PR #6)

## 完了したこと(第5幕以降の実装)

- `scripts/detect-pending.mjs`: 未投稿(検収合格・`published.json`なし)フォルダを1件検出
- `scripts/publish-instagram.mjs`: Instagram Graph APIへ投稿し、成功後 `published.json` を記録(既存 `src/publish.js` は無変更、`daily-post.yml` はそのまま依存し続ける)
- `.github/workflows/publish.yml`(新設): mainへの`push`(`posts/**`)または手動実行で上記を実行
- `.github/workflows/daily-post.yml`: 実処理は無変更、併存の注記コメントのみ追加
- `.claude/agents/inspector.md`: `posts/<dir>/` に置くファイル構成(`slides.json`+平らな`slide-N.png`)を追記

## 完了したこと(3Dオフィス表示層)

- `viewer/`: Vite + React + TypeScript + @react-three/fiber + @react-three/drei のローカル専用ビューア
  (和室オフィスのジオラマ、俯瞰45度前後の固定カメラ、低彩度の暖色ニュートラル、マット質感)
- キャラクターに顔(目・口・ほっぺ)・机に置いた手・靴先までの脚を追加、名札は机の前面に配置
- `logs/events.jsonl` のポーリング取得(`src/lib/eventLog.ts`)、actorごとの状態管理(`src/state/officeState.ts`)、
  7イベント(`start/progress/output/handoff/blocked/reject/done`)に応じたアバターの演出(`Avatar.tsx`)を実装
- `StatusPanel`/`ActivityPanel` を実データ連携(プレースホルダーから置き換え)
- `.claude/skills/office-view/SKILL.md` を実装済み状態に更新
- `logs/events.sample.jsonl`: 1本の投稿が第1〜5幕を通る、reject 1回込みのサンプルイベント列(26件)
- **既知の制限**: ページを開いた瞬間に過去の全イベントを一気に反映するため、同一社員の中間イベントは
  表示されず最終状態のみ反映される。`handoff`/`reject` は「身を乗り出す」動きのみで、机の間を実際に
  紙が飛ぶような演出は無い

## 参照ドキュメント

- 設計書(投稿トリガー): `docs/superpowers/specs/2026-08-12-act5-publish-trigger-design.md`
- 設計書(3Dオフィス表示層): `docs/superpowers/specs/2026-08-13-office-viewer-design.md`
- 設計書(カルーセルデザイン改修+解説キャラクター): `docs/superpowers/specs/2026-08-13-character-slide-design.md`
- 設計メモ(sources/work雛形): `docs/superpowers/specs/2026-08-13-sources-work-scaffold.md`
- 決定メモ(Issue自動起動化の見送り): `docs/superpowers/specs/2026-08-13-issue-auto-trigger-decision.md`
- 実装計画: `docs/superpowers/plans/2026-08-12-act5-publish-trigger.md`
- 会社の決まりごと: `AGENTS.md`
- 社員(エージェント)構成・幕の定義: `CLAUDE.scaffold.md`
- viewer/の使い方: `viewer/README.md`
