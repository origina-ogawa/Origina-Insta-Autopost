# 進捗

作業を始める前に必ずこのファイルを読むこと。作業の節目(タスク完了、方針決定、コミット・PR作成など)で必ず更新すること。
自動圧縮でスレッドの記憶が失われても、ここを読めば状況を復元できるようにする。

最終更新: 2026-08-13(カルーセルのデザイン改修+解説キャラクター機能を実装し、PR #8として提出)

## 今の状況

- 作業ブランチ: `feat/office-viewer-tatami`(base: `feat/ai-company`)
- PR: [#3 3Dオフィス表示層](https://github.com/origina-ogawa/Origina-Insta-Autopost/pull/3) は**マージ済み**
- PR: [#8 カルーセルの1要素デザイン化と解説キャラクター機能を追加](https://github.com/origina-ogawa/Origina-Insta-Autopost/pull/8)(未マージ、社長の確認待ち。base: `feat/ai-company`)
  - 本文スライドを「1スライド1要素」構成に変更(文字数削減・フォント拡大・余白改善)
  - 表紙・本文スライド右下に解説キャラクター(ポーズ違い6種、`assets/character/`)を追加。`config/brand.own.json`の`character.enabled`でON/OFF、無効時は既存アイコン表示にフォールバック
  - 設計書: `docs/superpowers/specs/2026-08-13-character-slide-design.md`、実装計画: `docs/superpowers/plans/2026-08-13-character-slide.md`
  - **記録**: `assets/mirai01.png`/`assets/mirai-ad.png`の人物は、社長ご自身がAIで生成した架空の人物とのご説明を前提に実装(実在人物の肖像は使用していない)
  - サブエージェントによるタスクごとのレビュー+最終ブランチレビュー(opus)済み。Important指摘3件を修正済み(表紙タイトル幅のスコープ漏れ、白背景の重なり、ポーズ画像のアスペクト比不統一)
- PR: [#2 第5幕以降(投稿トリガー)を実装](https://github.com/origina-ogawa/Origina-Insta-Autopost/pull/2)(未マージ、社長の確認待ち)
- 「幕」構成(researcher→director→producer→inspector→publisher)への移行のうち、**第5幕から先(検収合格→PR→mainマージ→Instagram投稿)は実装完了**。テスト15/15成功、コードレビュー(タスクごと+全体)済み

## 【確認してほしいこと】3Dオフィス表示層(viewer/)

- PR: [#3 3Dオフィス表示層を和室ジオラマ調で作り直す](https://github.com/origina-ogawa/Origina-Insta-Autopost/pull/3)(未マージ、base: `feat/ai-company`)。コミット5つ: ①見た目刷新 ②顔・手足の追加(社長フィードバック) ③名札を頭上→机の前面へ移動(社長フィードバック) ④ログ連携・イベント演出の実装 ⑤演出をSCHEMA.md準拠に作り直し(社長からの詳細指示)
- 設計書: `docs/superpowers/specs/2026-08-13-office-viewer-design.md`(末尾に方針転換の追記あり)
- 使い方: `npm --prefix viewer install` → `npm --prefix viewer run dev` → 表示されたURLをブラウザで開く
- **技術構成**: Vite + React + TypeScript + @react-three/fiber + @react-three/drei
- **アートディレクション**: 「和室オフィスのジオラマ」(畳/生成り色の床、木製デスク、低彩度の暖色ニュートラル、
  マット質感、俯瞰45度前後の固定カメラ)。キャラクターは顔(目・口・ほっぺ)と机に置いた手を持つ低ポリチビキャラ
- **`logs/events.jsonl` の読み込み・イベント演出を、社長の詳細指示(SCHEMA.md準拠、ease-in-out、控えめな動き)
  に沿って作り直した。** データ取得は `src/lib/eventLog.ts` でパース(`parseNewEvents`)と取得方式
  (`fetchPollingSource`)を分離し、将来 `fs.watch`+WebSocketへ差し替え可能な構造にした
- イベント対応(詳細は `viewer/README.md`):
  `start`=着席の弾み / `progress`=吹き出し / `output`=吹き出し+机に紙が増える+アクティビティログに
  ファイル名付きで追加(「成果物モニター」) / `handoff`=次の社員の方向へ身を乗り出す / `blocked`=色が
  くすむ+「!」バッジ / `reject`=赤フラッシュ+差し戻し先の方向へ身を乗り出す / `done`=フェードアウト+「✓」バッジ。
  すべてease-in-out・0.5〜0.7秒で統一し、過剰な演出は入れていない
- 未知のactor/eventは無視して続行(落ちない)。`handoff`/`reject` の宛先は `logs/SCHEMA.md` に明示フィールドが
  無いため、固定の幕の流れ(researcher→director→producer→inspector→publisher)から推測している
- **既知の制限**: ページを開いた瞬間に過去の全イベントを一気に反映するため、同一社員の中間イベントは
  表示されず最終状態のみ反映される(実運用でイベントが1件ずつ届く場合は個別に演出が発火する)。
  `handoff`/`reject` は「身を乗り出す」動きのみで、机の間を実際に紙が飛ぶような演出は無い
- Playwrightで動作確認: コンソールエラー無し・`logs/events.sample.jsonl` を使って
  start/progress/output/handoff/blocked/reject/doneの一連の演出が実際に発火し、アニメーション後に
  正しい位置へ戻ることを確認済み
- `logs/events.sample.jsonl`: 1本の投稿が第1〜5幕を通しで流れ、検収差し戻し(reject)が1回入るサンプル
  (26件、ticket `#101`)。動作確認に使用、本番の `logs/events.jsonl` は汚していない
- 「オーバーレイ4種」の中身についての以前の疑問は、この指示で「実データを表示する」という点は
  解決した(StatusPanel・ActivityPanelを実データ連携済み)。ただし4種の個別の内訳そのものは
  今回も明示されなかったため、引き続き前回の仮決め(タイトル/社員ステータス/アクティビティログ/凡例)
  のままにしている

## 残タスク

優先度の高い順。

1. **【社長】PR #8(カルーセル改修+解説キャラクター)のレビュー・マージ判断**(上記参照)
   - マージは社長のみが行う(AGENTS.md)
2. **【社長】PR #2 のレビュー・マージ判断**
   - マージは社長のみが行う(AGENTS.md)
3. **【社長確認後】`publish.yml` の `workflow_dispatch`(`dry_run: true`)動作確認**
   - `workflow_dispatch` はデフォルトブランチ(main)にファイルが無いと使えないため、PRマージ後でないと実施できない
4. **【要事前確認】実際の投稿を伴うテスト(`dry_run: false`)**
   - CLAUDE.mdの禁止事項により、必ず事前に社長へ確認してから実行する
5. **未着手: サブプロジェクト2「第1・2幕のディレクトリ運用整備」**
   - `sources/` の雛形、`work/<slug>/`、ブランチ運用(`post/YYYY-MM-DD-<slug>`)の実運用ルール確立
   - 設計書: `docs/superpowers/specs/2026-08-12-act5-publish-trigger-design.md` の「未解決事項」参照
6. **未着手: サブプロジェクト3「第2〜3幕のスキーマ統合」**
   - `producer.md` が書く `slides.json`(no/heading/body形式)と、既存 `render.js` が読む `post.json`(cover/body/summary+blocks形式)の食い違いを解消する
7. **未着手: 幕1〜4のIssue自動起動化の検討**
   - 現在は手動運用(社長がpmエージェントに指示)の想定。実績確認後に自動化するかどうかを判断する
8. **未着手: エージェントが実際に `scripts/emit-event.mjs` を呼ぶようにする**
   - viewer/がログ連携まで完成したことで初めて価値が出る。各エージェント定義(`.claude/agents/*.md`)に節目でのイベント発行を組み込む必要がある
9. **(任意・低優先度)viewer/の細かい改善候補**
   - `handoff` イベントで隣の机へ何かが飛ぶような専用演出(現状は吹き出しのみ)
   - ページ読み込み時の全イベント一括再生による中間状態の欠落(known limitation、README参照)
10. **(任意・低優先度)最終レビューで見つかった軽微な指摘の後始末**
    - `scripts/detect-pending.mjs` の `::warning::` メッセージが `workflow_dispatch` 実行時にも「pushイベントで」と表示される(誤解を招く文言だが機能に影響なし)
    - `DIR_PATTERN` 正規表現が `scripts/detect-pending.mjs` と `scripts/publish-instagram.mjs` の2箇所に重複している(意図的に許容、共有モジュール化はしていない)

## 完了したこと(カルーセルのデザイン改修+解説キャラクター機能)

- `src/lib/components.js`: 本文スライドを2カラム詰め込み→1カラム1要素構成に変更、文字数削減に合わせてフォント拡大
- `src/lib/characterPoses.js`: スライドへのポーズ画像割り当てロジック(summaryスライド除外・循環割当、単体テスト付き)
- `scripts/generate-character-poses.mjs`: 参考画像からポーズ違い画像を生成する手動スクリプト(自動投稿フローには非組込)
- `assets/character/pose-01〜06.png`: 事前生成したポーズ画像(6種、コミット済み)
- `src/render.js`: `character`設定を読み込み、スライドごとにポーズ画像をdata URIで埋め込み。ファイル欠落時はエラーで停止
- `config/brand.own.json`: `character`設定を追加(`enabled`/`dir`/`poses`)
- 最終ブランチレビュー(opus)で見つかったImportant 3件を修正: 表紙タイトルの幅制限がキャラクター無効ブランドにも適用されていた不具合、キャラクター画像の白背景がパネル上に矩形として見えていた問題(`mix-blend-mode: multiply`で解消)、ポーズ画像1枚だけアスペクト比が違いサイズが不揃いだった問題(クロップで統一)

## 完了したこと(第5幕以降の実装)

- `scripts/detect-pending.mjs`: 未投稿(検収合格・`published.json`なし)フォルダを1件検出
- `scripts/publish-instagram.mjs`: Instagram Graph APIへ投稿し、成功後 `published.json` を記録(既存 `src/publish.js` は無変更、`daily-post.yml` はそのまま依存し続ける)
- `.github/workflows/publish.yml`(新設): mainへの`push`(`posts/**`)または手動実行で上記を実行
- `.github/workflows/daily-post.yml`: 実処理は無変更、併存の注記コメントのみ追加
- `.claude/agents/inspector.md`: `posts/<dir>/` に置くファイル構成(`slides.json`+平らな`slide-N.png`)を追記

## 完了したこと(3Dオフィス表示層)

- `viewer/`: Vite + React + TypeScript + @react-three/fiber + @react-three/drei のローカル専用ビューア
  (和室オフィスのジオラマ、俯瞰45度前後の固定カメラ、低彩度の暖色ニュートラル、マット質感)
- キャラクターに顔(目・口・ほっぺ)・机に置いた手・靴先までの脚を追加、名札は机の前面に配置
- `logs/events.jsonl` のポーリング取得(`src/lib/eventLog.ts`)、actorごとの状態管理(`src/state/officeState.ts`)、
  7イベント(`start/progress/output/handoff/blocked/reject/done`)に応じたアバターの演出(`Avatar.tsx`)を実装
- `StatusPanel`/`ActivityPanel` を実データ連携(プレースホルダーから置き換え)
- `.claude/skills/office-view/SKILL.md` を実装済み状態に更新
- `logs/events.sample.jsonl`: 1本の投稿が第1〜5幕を通る、reject 1回込みのサンプルイベント列(26件)

## 参照ドキュメント

- 設計書(投稿トリガー): `docs/superpowers/specs/2026-08-12-act5-publish-trigger-design.md`
- 設計書(3Dオフィス表示層): `docs/superpowers/specs/2026-08-13-office-viewer-design.md`
- 実装計画: `docs/superpowers/plans/2026-08-12-act5-publish-trigger.md`
- 会社の決まりごと: `AGENTS.md`
- 社員(エージェント)構成・幕の定義: `CLAUDE.scaffold.md`
- viewer/の使い方: `viewer/README.md`
